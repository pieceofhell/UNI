#include <math.h>
#include <stdio.h>

int main() {
  float base, height, area, diag;
  printf("Digite o valor da diagonal do retangulo.\n");
  scanf("%f", &diag);
  diag = pow(base, 2) * pow(height, 2);

  printf("Area do retangulo: %.2f\n.", );
  return 0;
}