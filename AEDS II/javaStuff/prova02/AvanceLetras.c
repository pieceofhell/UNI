#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int n = 0;
    scanf("%d", &n);

    char array[105];
    char array1[105];

    // for (int i = 0; i < n; i++) {
    //     int movs = 0;
    //     scanf("%s %s", array, array1);
    //     for (int j = 0; j < strlen(array); j++) {
    //         while (array[j] != array1[j]) {
    //             if (array[j] == 'z') {
    //                 array[j] = 'a';
    //             } else {
    //                 array[j]++;
    //             }
    //             movs++;
    //         }
    //     }
    //     printf("%d", movs);
    // }

    

    return 0;
}
